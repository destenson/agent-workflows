# Known bugs

_Open, unfixed bugs for this project. Each entry stays until the bug is fixed, then it is removed — this log tracks only what is still broken, not a history of everything ever fixed. Durable project state: commit it._

<!--
Add one entry per bug, newest first. Drop the entry once the bug is fixed.
Keep each entry short enough to act on; link to issues/PRs if you have them.
-->

## {short bug title}
- **Found:** {date}
- **Where:** {file / module / command / URL where it shows up}
- **Symptom:** {what goes wrong, observably}
- **Repro:** {the smallest steps or input that trigger it, if known}
- **Notes:** {suspected cause, why it was deferred, anything that will save the next person time — optional}
