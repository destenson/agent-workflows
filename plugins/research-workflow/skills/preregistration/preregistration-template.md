# Preregistration: <title>

> **Registered:** <YYYY-MM-DD HH:MM — timestamp when this was committed, not when it was drafted>
> **Author:** <name>
> **Experiment plan:** <link>
> **Scope note:** <if any data has already been seen, state clearly which analyses this preregistration applies to and which it does not>

## Hypothesis

Restate the hypothesis from the experiment plan exactly.

## Primary analysis

The specific computation or comparison that will evaluate the hypothesis. State it precisely enough that two people given the same data would run the same analysis without further instruction.

## Decision rule

Given the primary analysis result, the hypothesis is **confirmed** if: <threshold or criterion>
The hypothesis is **falsified** if: <threshold or criterion>
The result is **inconclusive** if: <conditions that do not meet either criterion>

This must match the experiment plan's stopping rule.

## Secondary analyses (planned)

Analyses that will be run but do not drive the primary decision. Results from these will be labeled as planned-secondary in the results memo.

Any analysis not listed here will be labeled exploratory in the results memo.

## Exclusion criteria

Conditions under which data points, runs, or trials will be excluded from analysis. Must be specified before data is seen.

| Exclusion criterion | Reason |
|--------------------|--------|
| | |

## Sample size / stopping rule

How many runs, trials, or samples will be collected, or the stopping rule that determines when collection ends. Reference the experiment plan's stopping rule if applicable.

## Where this is timestamped

The location of the immutable record of this document (version control commit, experiment tracker entry, etc.). A preregistration without a timestamp is not a preregistration.
