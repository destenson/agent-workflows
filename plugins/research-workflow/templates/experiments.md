# Experiments — {project title}

A running index of experiments: what was run, against which hypothesis, and where the detail lives. This is the top-level summary that loads into context; bulky per-run logs, configs, and outputs live in subdirectories (e.g. `research/experiments/<id>/`) and are not auto-loaded.

The `experiment-design` and `preregistration` skills produce the committed plan for each entry before it runs.

<!-- One row per experiment, newest first. -->

## {experiment id} — {short title}
- **Tests**: which hypothesis (from the proposal) this gathers evidence about
- **Plan**: metric, baseline, ablations, stopping rule — committed before running (link to the preregistered plan / detail dir)
- **Status**: planned · running · done · abandoned
- **Outcome (one line)**: what it showed, once known — full synthesis goes in results
- **Detail**: pointer to the run log / config / output location
