Before this session ends, distill what a future session would otherwise re-learn the hard way.

- Decisions: append any non-obvious choice made this session and its rationale — a method picked, a scope cut, a ship/iterate/kill call.
- Results / negative results: append what was learned, and especially every hypothesis falsified or approach abandoned this session — what was tried, why it failed, what it rules out. These are the records most often dropped and most valuable to reuse.
- Framing: if the research question or a live hypothesis changed this session, update the abstract/proposal so the next session starts from the corrected framing, not the stale one.

Record in proportion to what was learned, not the size of the session. Many sessions produce nothing worth keeping — if this one did, say "no entries" explicitly. Declaring empty is the normal, frictionless outcome; do not invent filler to satisfy this prompt.
