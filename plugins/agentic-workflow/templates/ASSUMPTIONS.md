# Assumptions

Status values: `unvalidated`, `validated-as-of`, `falsified`.
Gate: no implementation while any high-cost assumption is `unvalidated`. A `falsified` entry requires a SPEC correction before it is closed. `validated-as-of` records the spike, the data, and the date — validation is never terminal; the assumption returns to scrutiny when those conditions change.

| # | Assumption | Cost if false | Status | Validation | Evidence / spike |
|---|-----------|---------------|--------|------------|------------------|
| 1 | {one-sentence premise} | high | unvalidated | {cheapest test} | — |
