Before this session ends, distill what a future session would otherwise re-learn the hard way.

- DECISIONS.md: append any non-obvious choice made this session and its rationale.
- LESSONS.md: append every dead end hit this session — what was tried, why it failed, what to do instead. Prioritize anything that is NOT visible in the code or spec.

Record in proportion to what was learned, not the size of the diff. Most sessions produce nothing worth keeping — if this one did, say "no entries" explicitly. Declaring empty is the normal, frictionless outcome; do not invent filler to satisfy this prompt.
