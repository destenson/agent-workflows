#!/usr/bin/env bash
# Contract: close one direction of the TCP connection only.
# Exercises: half-open connection handling (reads hang, writes "succeed").
# Usage:   faults/rtsp-half-close.sh
# Exit:    0 once armed; non-zero if the server isn't running.
set -euo pipefail
echo "TODO: implement rtsp-half-close.sh on the fault server" >&2
exit 2
